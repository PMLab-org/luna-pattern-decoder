## Plan

1. In `src/routes/pricing.tsx`, update the Free plan `features` array:
   - Change `"Unlimited symptom tracking"` → `"Limited symptom tracking"`
   - Append `"Real, useful tool — not a teaser"` as a fifth item

No other files need changes. The `src/components/pricing-preview.tsx` only shows blurbs, not the detailed feature list.