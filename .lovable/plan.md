## Swap header layout

In `src/components/site-header.tsx`, swap the left and right groups:

- **Left**: `Luna by PM Lab` wordmark + logo (links to `/`) — same markup currently on the right.
- **Right**: `Pricing` and `For Clinicians` nav links, followed by the `Start Your First Entry` CTA button.

No other files change. Styling, tokens, sticky behavior, and active states stay the same.